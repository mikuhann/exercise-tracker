export const selectExercises = ({ exercise }) => exercise.exercises;
export const selectLoading = ({ exercise }) => exercise.loading;
export const selectUsersLoading = ({ users }) => users.loading;
export const selectUsers = ({ users }) => users.users;
